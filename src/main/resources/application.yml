spring:
  application:
    name: eureka-server

server:
  port: 8761

eureka:
#  instance:
#    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://202.178.125.77:1761/eureka/

